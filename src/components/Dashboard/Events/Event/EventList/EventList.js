import React, {Component} from 'react'
import Where from '../../../Conditions/Where/Where'

const EventList  = (props) => { 
  return (
      
        <li>{props.eventName}</li>
      
      )
}
export default EventList;